hosts:
  production:
    hostname: production.server.org
    remote_user: username
    http_user: username
    bin/php: /usr/local/php82/bin/php
    writable_mode: chmod
    deploy_path: ~/path_to_the_project
  test:
    hostname: test.server.org
    remote_user: username
    http_user: username
    bin/php: /usr/local/php82/bin/php
    writable_mode: chmod
    deploy_path: ~/path_to_the_project
